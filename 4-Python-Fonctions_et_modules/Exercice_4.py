# -*- coding: windows-1252 -*-
#-----------------------------------------------------------------------------
# Exercice 4 - Vecteur et produit scalaire
#
# Tous les vecteurs considérés sont des vecteurs réels.
# (1) Définir une fonction somme(u, v) qui calcule et retourne la somme de deux
# vecteurs donnés en paramètre.
# (2) Définir une fonction mult(a, u) qui calcule et retourne le produit du
# vecteurs u par le réel a.
# (3) Ecrire le script qui affiche le résultat de a.u + b.v où a, b sont des
# réels donnés par l'utilisateur et u = (1, 2, 1, 3) et v = (4, 1, 0, 2).
# (4) Définir une fonction produit_scalaire(u,v) qui calcule et retourne le
# produit scalaire de deux vecteurs donnés en paramètres. Notons que pour faire
# le produit scalaire les vecteurs doivent avoir le même nombre de composantes.
# (5) Définir la fonction norme(u) qui calcule et retourne la norme du vecteur
# u donné en paramètre.
# (6) Définir la fonction cosinus(u, v) qui calcule et retourne le cosinus de
# l'angle (u, v).
# (7) Définir une fonction saisit_vecteur(n) qui retourne le vecteur de n
# coordonnées données par l'utilisateur dans le corps de la fonction.
# (8) Ecrire le script qui saisit deux vecteurs x, y de dimension 3, qui
# affiche les vecteurs, leur norme, leur produit scalaire et le cosinus de
# l'angle formé par ces deux vecteurs.
#-----------------------------------------------------------------------------
